{
  "buildCommand": "cd src && npm run build",
  "outputDirectory": "src/.next",
  "env": {
    "NEXT_PUBLIC_ENABLE_CONTACT_FORM": "@next_public_enable_contact_form",
    "SMTP_HOST": "@smtp_host",
    "SMTP_PORT": "@smtp_port",
    "SMTP_USER": "@smtp_user",
    "SMTP_PASS": "@smtp_pass",
    "CONTACT_RECIPIENT": "@contact_recipient"
  }
}
